%title 在vim中插入日期

=在vim中插入日期=

日期: 2012-03-30 14:03

在vi中插入日期,可以使用下列命令：

     `:r!date \+"\%Y-\%m-\%d"`
     
上面的`\`用于转义。

也可定义好一个命令别名，将日期格式化好，后面可以直接使用：

     `alias datef='date +"%Y-%m-%d"'`
     
vi中可以直接用

     `:r!datef`

