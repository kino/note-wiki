两次解决了HBase RegionServer内存溢出的问题，并成功找到了影响内存溢出的操作系统参数，并提供了测试报告。
积极配合了Dashboard2.0性能调优的工作，在写入端实现配置进行双写，最终实现新老版本平稳过渡。
增加了Collectors的FIFO功能，并通过配置参数进行开关。
CLog中增加了日志数据的路由功能。
在经过了一年的开发，且迁移，扩容，升级后，进行了代码整理，去除了不需要的代码，比如为了升级而存在的新老版本代码与相应的配置项。
对各业务团队完成了Hive使用培训。
完成了Trace的规划与设计文档。
团队队员完成了Redis的性能测试报告与评估。


一年来，负责的CLog与Hadoop也算做出了一定的成绩。
对于CLog，完成了RestApi化，LogBrowser的开发。接入应用数从最初的几十，到现在的接近八百个应用。
数据流量从最初的几十G，到现在的3.5T/Day(规划为2T/Day), 其中经历了大量的性能调优以支持数据量的增长，并在HBase的应用开发与schema设计方面积累了大量经验。

Hadoop集群从最初的10台增加到50台。并将集群从福泉迁移到了金桥，并完成了老数据的迁移，当然主要功劳是具体负责迁移的同事。
在扩容的过程中解决大量的HadoopHbase的问题，比如RegionServer集体down机，Hdoop集体down机，region损坏恢复，是最主要的bug排查者，最坏的一次三天睡了两个小时。
但也导致了一些问题，导致8月份根本没有时间面试，导致团队成员一直不足。
